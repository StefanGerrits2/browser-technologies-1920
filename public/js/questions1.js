if (window.localStorage) {
    console.log('window localstorage is enabled');

    // var age = document.querySelector('#age');

    // localStorage.setItem('age', JSON.stringify(age.value));

    // age.value = JSON.parse(localStorage.getItem('age'));
}